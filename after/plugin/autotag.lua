require('nvim-ts-autotag').setup(
    {
        enable = true,
        enable_rename = true,
        enable_close = true,
        filetypes = { "htmx", "jsx", "html", "javascript", "tsx", "typescript", "xml" }
    }
)
